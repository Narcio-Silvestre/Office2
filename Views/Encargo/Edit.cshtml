

<body>
    <div class="container ">
        <main>
            <div class="row g-5 justify-content-center mt-1 ">

                    
                    <div id="box-2" class="col-md-7 col-lg-8 py-5 bg-light shadow-lg mt-3 table-responsive justify-content-center">


                        <table class="table table-hover tablee">
                            <thead>
                                <tr>
                                    <th scope="col">Encargo</th>
                                    <th scope="col">Requerente</th>
                                    <th scope="col">Molde</th>
                                    <th scope="col">Data</th>
                                    <th scope="col"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (ViewBag.encargo != null)
                            {
                                @for (int x = 0; x < ViewBag.encargo.Count; x++)
                                {
                                    if (@ViewBag.encargo[x].estadoId == 2)
                                    {
                                        <tr>
                                            <td>@ViewBag.encargo[x].nrEncargo</td>
                                            <td>@ViewBag.encargo[x].entidade</td>
                                            <td>@ViewBag.encargo[x].molde</td>
                                            <td>@ViewBag.encargo[x].data</td>
                                            <td>
                                                <div style="display:flex; justify-content:center; gap:2px;">
                                                    <a asp-action="Edit2" asp-controller="Encargo" asp-route-id="@ViewBag.encargo[x].id" class="btn btn-sm btn-warning"> <i class="bi bi-pencil-square"></i></a>
                                                    @if (@ViewBag.UserFuncId == 1)
                                                    {
                                                        <a role="button" asp-action="Delete" asp-controller="Encargo" asp-route-id="@ViewBag.encargo[x].id" class="btn btn-sm  btn-danger"> <i class="bi bi-trash"></i></a>

                                                    }
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                }
                            }
                        </tbody>
                        </table>
                </div>

                    
                   
            </div>
        </main>
    </div>
</body>
